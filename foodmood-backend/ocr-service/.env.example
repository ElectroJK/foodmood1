PORT=4100
NODE_ENV=development
CORS_ORIGIN=http://localhost:4000
TESSERACT_LANG=eng
# Default category to assign to OCR-detected items
DEFAULT_CATEGORY=Other
# Default expiry offset in days when receipt doesn't include explicit expiry
DEFAULT_EXPIRY_OFFSET_DAYS=7
